import React from "react";

const UserBooking = () => {
    return (
        <div>
            <h1 style={{marginTop: '100px'}}>This is the User Menu</h1>
        </div>
    );
};

export default UserBooking;
